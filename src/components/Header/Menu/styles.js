import { styled } from '@mui/material';

export const Ul = styled.ul`
  list-style: none;
  margin: 0;

  display: flex;
  gap: 3rem;
  align-items: center;

  position: fixed;
  background-color: teal;
  height: 3rem;
  width: 100%;
`;
